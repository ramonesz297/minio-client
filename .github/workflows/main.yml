name: Publish

on:
  release:
    types: [published]

jobs:            
  publish:
    name: build, pack & publish
    runs-on: windows-latest
    environment:
      name: main
    steps:
      - uses: actions/checkout@v2
          
      - name: Setup dotnet
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x
      - name: tests
        run: dotnet test .\Minio.Client.Test\Minio.Client.Test.csproj -c Release --logger trx --results-directory out-tests
      
      - name: report
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
            name: test run
            path: out-tests/*.trx    
            reporter: dotnet-trx
            fail-on-error: 'true'
      
      - name: pack
        if: success()
        run: dotnet pack .\Minio.Client.Test\Minio.Client.csproj -c Release -o out --include-symbols --include-source
  
      - name: publish
        if: success()
        run: dotnet nuget push .\out\*.nupkg --source https://api.nuget.org/v3/index.json --api-key ${{secrets.NUGET_ACCESS_TOKEN}} --skip-duplicate
